x-keys: &keys
  volumes:
    - "../scripts/${NETWORK}/keys:/keys"
  working_dir: /keys

services:
  hydra-key-gen:
    <<: *keys
    image: ${HYDRA_IMAGE}
    restart: "no"
    command:
      [
        "gen-hydra-key",
        "--output-file",
        "./${INSTANCE}.hydra"
      ]
  cardano-key-gen:
    <<: *keys
    image: ${CARDANO_IMAGE}
    restart: "no"
    command:
      [
        "cli",
        "address",
        "key-gen",
        "--verification-key-file",
        "./${INSTANCE}.cardano.vk",
        "--signing-key-file",
        "./${INSTANCE}.cardano.sk"
      ]
  cardano-addr-gen-mainnet:
    <<: *keys
    image: ${CARDANO_IMAGE}
    restart: "no"
    command:
      [
        "cli",
        "address",
        "build",
        "--payment-verification-key-file",
        "./${INSTANCE}.cardano.vk",
        "--out-file",
        "./${INSTANCE}.cardano.addr",
        "--mainnet"
      ]
  cardano-addr-gen-testnet:
    <<: *keys
    image: ${CARDANO_IMAGE}
    restart: "no"
    command:
      [
        "cli",
        "address",
        "build",
        "--payment-verification-key-file",
        "./${INSTANCE}.cardano.vk",
        "--out-file",
        "./${INSTANCE}.cardano.addr",
        "--testnet-magic",
        "${CARDANO_TESTNET_MAGIC:-1}"
      ]